[project]
name = "slack-mcp"
version = "0.1.0"
description = "Remote (streamable-http) Slack MCP server (read channel history, post messages) for the TrueForge incident-responder agent"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.27.2,<2",
    "python-dotenv>=1.0.1",
    "slack-sdk>=3.33.0",
]
