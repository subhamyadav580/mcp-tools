[project]
name = "elastic-mcp"
version = "0.1.0"
description = "Remote (streamable-http) read-only Elasticsearch MCP server for the TrueForge incident-responder agent"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "elasticsearch==8.15.0",
    "httpx>=0.28.1",
    "mcp[cli]>=1.27.2",
    "python-dotenv>=1.0.1",
]
